<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.TextArea?>
<?import javafx.scene.layout.AnchorPane?>


<AnchorPane prefHeight="400.0" prefWidth="381.0" xmlns="http://javafx.com/javafx/10.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="seg.java.controllers.FaqController">
   <children>
      <TextArea editable="false" maxHeight="400.0" maxWidth="408.0" minHeight="368.0" minWidth="408.0" prefHeight="400.0" prefWidth="408.0" text="• How to re-declare a runway?&#10;Select a runway using a). Then select an obstacle using c) or input custom values in the corresponding fields. Finally click d) to re-declare.  &#10;&#10;• How to view the calculations’ breakdown?&#10;Once the runway has been re-declared click l) to see the calculations.&#10;&#10;• How to use the animation?&#10;The animations are available on ”Side-on view” and ”Both views” tabs.  &#10;Once the runway has been re-declared click e)/f) to start the animation &#10;and g) to remove the plane image generated by the animation.&#10;&#10;• How to email/save a pdf with the re-declared values?&#10;Once the runway has been re-declaredto save - click ”File” and ”Export via PDF” then select the destination.to email - click ”File” and ”Share via PDF” then type your email.&#10;&#10;• How to re-declare a runway with a custom obstacle?&#10;Select a runway using a) then type in obstacle details in the ”obstacle &#10;section” and click d) to re-declare.&#10;&#10;• How to change the colour scheme for colour-blind users?&#10;Select ”Configure” from the navigation bar and then click ”Change &#10;colour scheme”.&#10;&#10;• How to edit/add/save/delete a runway?&#10;Select &quot;Configure&quot; from the navigation bar then click &quot;Configure Airports / Runways&quot; and then a screen shall appear showing all the runways. Once a runway has been selected you are then able to press the 'Delete' and 'Edit' buttons. To add a new runway the button 'Add' should be pressed.&#10;" wrapText="true" />
   </children>
</AnchorPane>
